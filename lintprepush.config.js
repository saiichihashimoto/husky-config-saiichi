module.exports = {
	tasks: {
		/*
		 * TODO Test on prepush
		 * BODY The filenames get attached as args, no real way to do this
		 * '*': []
		 *     .concat(test(pkg))
		 *     .concat(build(pkg)),
		 */
	},
};
